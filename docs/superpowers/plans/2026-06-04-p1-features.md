# P1功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现P1功能，包括自动创建项目文件夹、文件实际存储、AI自动分类和设置页面

**Architecture:** 在现有Tauri+React架构基础上，新增文件存储服务和AI分类服务，复用智谱AI API，配置存储在本地JSON文件中

**Tech Stack:** Rust (Tauri), React, TypeScript, Ant Design, 智谱AI API

---

## 文件结构

### 后端文件（Rust）

| 文件路径 | 职责 | 操作 |
|----------|------|------|
| `src-tauri/src/commands/project.rs` | 项目命令 | 修改：添加文件夹创建逻辑 |
| `src-tauri/src/commands/file.rs` | 文件命令 | 修改：支持文件内容保存 |
| `src-tauri/src/commands/mod.rs` | 命令模块 | 修改：导出新命令 |
| `src-tauri/src/services/file_storage.rs` | 文件存储服务 | 新建 |
| `src-tauri/src/services/file_classifier.rs` | AI分类服务 | 新建 |
| `src-tauri/src/services/config_service.rs` | 配置服务 | 新建 |
| `src-tauri/src/services/mod.rs` | 服务模块 | 新建 |
| `src-tauri/src/config.rs` | 配置结构 | 修改：添加分类prompt字段 |
| `src-tauri/src/lib.rs` | 库入口 | 修改：注册新命令和服务 |
| `src-tauri/Cargo.toml` | 依赖配置 | 修改：添加base64依赖 |

### 前端文件（TypeScript）

| 文件路径 | 职责 | 操作 |
|----------|------|------|
| `src/services/fileService.ts` | 文件服务 | 修改：添加分类API |
| `src/services/configService.ts` | 配置服务 | 新建 |
| `src/components/FileManager/FileManager.tsx` | 文件管理器 | 修改：集成AI分类 |
| `src/components/FileManager/ClassifyModal.tsx` | 分类确认对话框 | 新建 |
| `src/components/Settings/SettingsPage.tsx` | 设置页面 | 新建 |
| `src/components/Settings/PromptEditor.tsx` | Prompt编辑器 | 新建 |
| `src/App.tsx` | 应用入口 | 修改：添加设置路由 |
| `src/types/index.ts` | 类型定义 | 修改：添加新类型 |

---

## Task 1: 自动创建项目文件夹

**Files:**
- Modify: `src-tauri/src/commands/project.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 修改create_project命令，添加文件夹创建逻辑**

```rust
// src-tauri/src/commands/project.rs
use crate::db::Database;
use crate::db::models::Project;
use tauri::State;
use std::path::Path;

#[tauri::command]
pub async fn create_project(
    db: State<'_, Database>,
    name: String,
    description: Option<String>,
) -> Result<Project, String> {
    let conn = db.conn.lock().await;

    conn.execute(
        "INSERT INTO projects (name, description) VALUES (?1, ?2)",
        rusqlite::params![name, description],
    )
    .map_err(|e| e.to_string())?;

    let id = conn.last_insert_rowid();

    // 创建项目文件夹结构
    let project_dir = format!("projects/{}", name);
    let stages = [
        "售前", "启动", "需求", "方案", "构建",
        "测试", "上线", "验收", "转客户成功", "关闭"
    ];

    for stage in stages {
        let stage_path = format!("{}/{}", project_dir, stage);
        std::fs::create_dir_all(&stage_path)
            .map_err(|e| format!("创建文件夹失败: {}", e))?;
    }

    eprintln!("[PROJECT] 创建项目文件夹: {}", project_dir);

    Ok(Project {
        id: Some(id),
        name,
        description,
        stage: "blueprint".to_string(),
        created_at: None,
        updated_at: None,
        status: "active".to_string(),
    })
}
```

- [ ] **Step 2: 测试文件夹创建**

```bash
# 启动应用，创建一个测试项目
# 检查projects目录下是否创建了对应的文件夹结构
ls -la projects/
```

Expected: 看到以项目名命名的文件夹，包含10个子文件夹

- [ ] **Step 3: 提交代码**

```bash
git add src-tauri/src/commands/project.rs
git commit -m "feat: 创建项目时自动创建文件夹结构"
```

---

## Task 2: 创建文件存储服务

**Files:**
- Create: `src-tauri/src/services/mod.rs`
- Create: `src-tauri/src/services/file_storage.rs`
- Modify: `src-tauri/Cargo.toml`

- [ ] **Step 1: 添加base64依赖到Cargo.toml**

```toml
# src-tauri/Cargo.toml
[dependencies]
# ... existing dependencies
base64 = "0.21"
```

- [ ] **Step 2: 创建services模块**

```rust
// src-tauri/src/services/mod.rs
pub mod file_storage;
pub mod file_classifier;
pub mod config_service;
```

- [ ] **Step 3: 实现文件存储服务**

```rust
// src-tauri/src/services/file_storage.rs
use std::path::Path;
use base64::{Engine as _, engine::general_purpose::STANDARD as BASE64};

/// 保存文件到磁盘
///
/// # Arguments
/// * `project_name` - 项目名称
/// * `stage` - 项目阶段
/// * `file_name` - 文件名
/// * `content_base64` - base64编码的文件内容
///
/// # Returns
/// 文件保存的相对路径
pub fn save_file(
    project_name: &str,
    stage: &str,
    file_name: &str,
    content_base64: &str,
) -> Result<String, String> {
    // 解码base64内容
    let content = BASE64.decode(content_base64)
        .map_err(|e| format!("base64解码失败: {}", e))?;

    // 构建文件路径
    let file_path = format!("projects/{}/{}/{}", project_name, stage, file_name);

    // 确保目录存在
    if let Some(parent) = Path::new(&file_path).parent() {
        std::fs::create_dir_all(parent)
            .map_err(|e| format!("创建目录失败: {}", e))?;
    }

    // 写入文件
    std::fs::write(&file_path, &content)
        .map_err(|e| format!("写入文件失败: {}", e))?;

    eprintln!("[FILE_STORAGE] 保存文件: {}", file_path);

    Ok(file_path)
}

/// 读取文件内容（base64编码）
pub fn read_file(file_path: &str) -> Result<String, String> {
    let content = std::fs::read(file_path)
        .map_err(|e| format!("读取文件失败: {}", e))?;

    Ok(BASE64.encode(&content))
}

/// 检查文件是否存在
pub fn file_exists(file_path: &str) -> bool {
    Path::new(file_path).exists()
}

/// 删除文件
pub fn delete_file(file_path: &str) -> Result<(), String> {
    if Path::new(file_path).exists() {
        std::fs::remove_file(file_path)
            .map_err(|e| format!("删除文件失败: {}", e))?;
    }
    Ok(())
}
```

- [ ] **Step 4: 更新lib.rs，添加services模块**

```rust
// src-tauri/src/lib.rs
pub mod commands;
pub mod config;
pub mod db;
pub mod services;  // 添加这一行
```

- [ ] **Step 5: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 6: 提交代码**

```bash
git add src-tauri/src/services/mod.rs src-tauri/src/services/file_storage.rs src-tauri/Cargo.toml src-tauri/src/lib.rs
git commit -m "feat: 添加文件存储服务"
```

---

## Task 3: 修改文件命令，支持文件内容保存

**Files:**
- Modify: `src-tauri/src/commands/file.rs`
- Modify: `src-tauri/src/commands/mod.rs`

- [ ] **Step 1: 修改create_file命令**

```rust
// src-tauri/src/commands/file.rs
use crate::db::Database;
use crate::db::models::File;
use crate::services::file_storage;
use tauri::State;

#[tauri::command]
pub async fn create_file(
    db: State<'_, Database>,
    project_id: i64,
    name: String,
    path: String,
    category: Option<String>,
    content: Option<String>,  // 新增：base64编码的文件内容
    project_name: Option<String>,  // 新增：项目名称
    stage: Option<String>,  // 新增：项目阶段
) -> Result<File, String> {
    let conn = db.conn.lock().await;

    // 如果提供了文件内容，保存到磁盘
    let actual_path = if let (Some(content), Some(project_name), Some(stage)) = (&content, &project_name, &stage) {
        file_storage::save_file(&project_name, &stage, &name, content)?
    } else {
        path
    };

    conn.execute(
        "INSERT INTO files (project_id, name, path, category) VALUES (?1, ?2, ?3, ?4)",
        rusqlite::params![project_id, name, actual_path, category],
    )
    .map_err(|e| e.to_string())?;

    let id = conn.last_insert_rowid();

    Ok(File {
        id: Some(id),
        project_id,
        name,
        path: actual_path,
        category,
        tags: None,
        version: 1,
        content_hash: None,
        created_at: None,
        updated_at: None,
    })
}

// ... 其他命令保持不变
```

- [ ] **Step 2: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 3: 提交代码**

```bash
git add src-tauri/src/commands/file.rs
git commit -m "feat: 文件命令支持内容保存"
```

---

## Task 4: 创建AI分类服务

**Files:**
- Create: `src-tauri/src/services/file_classifier.rs`
- Modify: `src-tauri/src/config.rs`

- [ ] **Step 1: 更新AppConfig结构，添加分类prompt**

```rust
// src-tauri/src/config.rs
/// 应用配置
pub struct AppConfig {
    /// 智谱AI API Key
    pub zhipu_api_key: String,
    /// 智谱AI API 端点
    pub zhipu_api_url: String,
    /// 模型名称
    pub model_name: String,
    /// 分类prompt
    pub classification_prompt: String,
}

impl Default for AppConfig {
    fn default() -> Self {
        Self {
            zhipu_api_key: "da2c0511ffbc4abe8f66533c6c35f1e6.Xs2mvQ1RMYdDq7Se".to_string(),
            zhipu_api_url: "https://open.bigmodel.cn/api/paas/v4/chat/completions".to_string(),
            model_name: "glm-4.7-flash".to_string(),
            classification_prompt: r#"你是一个专业的文档分类专家。请根据以下文档内容，判断它属于哪个类别：

类别：
- 售前：销售资料、客户沟通、报价单
- 启动：项目启动会、章程、团队组建
- 需求：需求文档、用户故事、用例
- 方案：技术方案、架构设计、选型
- 构建：开发文档、代码规范、接口定义
- 测试：测试用例、测试报告、缺陷
- 上线：部署文档、发布说明、运维
- 验收：验收标准、验收报告、签字
- 转客户成功：交接文档、培训资料、FAQ
- 关闭：项目总结、复盘、归档

文档内容：
{content}

请返回以下JSON格式：
{
  "category": "类别名称",
  "confidence": 0.95,
  "summary": "文档内容摘要（50字以内）",
  "file_type": "文件类型（如：需求文档、测试报告等）"
}"#.to_string(),
        }
    }
}
```

- [ ] **Step 2: 实现AI分类服务**

```rust
// src-tauri/src/services/file_classifier.rs
use serde::{Deserialize, Serialize};

/// 分类结果
#[derive(Debug, Serialize, Deserialize)]
pub struct ClassificationResult {
    pub category: String,
    pub confidence: f32,
    pub summary: String,
    pub file_type: String,
}

/// AI请求结构
#[derive(Serialize)]
struct AIRequest {
    model: String,
    messages: Vec<AIMessage>,
    max_tokens: u32,
    temperature: f32,
}

#[derive(Serialize)]
struct AIMessage {
    role: String,
    content: String,
}

/// AI响应结构
#[derive(Deserialize)]
struct AIResponse {
    choices: Option<Vec<AIChoice>>,
    error: Option<AIError>,
}

#[derive(Deserialize)]
struct AIChoice {
    message: Option<AIMessageResponse>,
}

#[derive(Deserialize)]
struct AIMessageResponse {
    content: String,
}

#[derive(Deserialize)]
struct AIError {
    message: Option<String>,
}

/// 调用AI进行文件分类
///
/// # Arguments
/// * `file_content` - 文件内容（文本）
/// * `prompt` - 分类prompt模板
/// * `api_key` - 智谱AI API Key
/// * `api_url` - 智谱AI API 端点
/// * `model` - 模型名称
///
/// # Returns
/// 分类结果
pub async fn classify_file(
    file_content: &str,
    prompt: &str,
    api_key: &str,
    api_url: &str,
    model: &str,
) -> Result<ClassificationResult, String> {
    let http_client = reqwest::Client::new();

    // 构建prompt，替换{content}占位符
    let full_prompt = prompt.replace("{content}", file_content);

    // 构建请求
    let request = AIRequest {
        model: model.to_string(),
        messages: vec![
            AIMessage {
                role: "user".to_string(),
                content: full_prompt,
            },
        ],
        max_tokens: 1024,
        temperature: 0.3,
    };

    eprintln!("[CLASSIFIER] 调用AI分类API");

    // 发送请求
    let response = http_client
        .post(api_url)
        .header("Content-Type", "application/json")
        .header("Authorization", format!("Bearer {}", api_key))
        .json(&request)
        .send()
        .await
        .map_err(|e| format!("API请求失败: {}", e))?;

    let response_text = response.text().await
        .map_err(|e| format!("读取响应失败: {}", e))?;

    eprintln!("[CLASSIFIER] 收到响应: {}", &response_text[..response_text.len().min(200)]);

    // 解析响应
    let ai_response: AIResponse = serde_json::from_str(&response_text)
        .map_err(|e| format!("解析响应失败: {}", e))?;

    // 检查错误
    if let Some(error) = ai_response.error {
        return Err(format!("AI返回错误: {:?}", error.message));
    }

    // 提取回复内容
    let reply = ai_response.choices
        .as_ref()
        .and_then(|c| c.first())
        .and_then(|c| c.message.as_ref())
        .map(|m| m.content.clone())
        .ok_or_else(|| "无法获取AI回复".to_string())?;

    // 解析JSON结果
    let result: ClassificationResult = serde_json::from_str(&reply)
        .map_err(|e| format!("解析分类结果失败: {}. 原始回复: {}", e, reply))?;

    eprintln!("[CLASSIFIER] 分类结果: category={}, confidence={}", result.category, result.confidence);

    Ok(result)
}
```

- [ ] **Step 3: 添加reqwest依赖（如果还没有）**

```toml
# src-tauri/Cargo.toml
[dependencies]
# ... existing dependencies
reqwest = { version = "0.11", features = ["json"] }
```

- [ ] **Step 4: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 5: 提交代码**

```bash
git add src-tauri/src/services/file_classifier.rs src-tauri/src/config.rs
git commit -m "feat: 添加AI文件分类服务"
```

---

## Task 5: 创建分类命令

**Files:**
- Create: `src-tauri/src/commands/classify.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 创建分类命令**

```rust
// src-tauri/src/commands/classify.rs
use crate::config::AppConfig;
use crate::services::file_classifier::{self, ClassificationResult};
use crate::services::file_storage;
use tauri::State;

#[tauri::command]
pub async fn classify_file(
    config: State<'_, AppConfig>,
    file_path: String,
) -> Result<ClassificationResult, String> {
    // 读取文件内容
    let content_base64 = file_storage::read_file(&file_path)?;

    // 解码base64
    use base64::{Engine as _, engine::general_purpose::STANDARD as BASE64};
    let content_bytes = BASE64.decode(&content_base64)
        .map_err(|e| format!("base64解码失败: {}", e))?;

    let content = String::from_utf8_lossy(&content_bytes).to_string();

    // 调用AI分类
    let result = file_classifier::classify_file(
        &content,
        &config.classification_prompt,
        &config.zhipu_api_key,
        &config.zhipu_api_url,
        &config.model_name,
    ).await?;

    Ok(result)
}
```

- [ ] **Step 2: 更新commands/mod.rs**

```rust
// src-tauri/src/commands/mod.rs
pub mod ai;
pub mod classify;
pub mod file;
pub mod project;
```

- [ ] **Step 3: 更新lib.rs，注册新命令**

```rust
// src-tauri/src/lib.rs
pub mod commands;
pub mod config;
pub mod db;
pub mod services;

use tauri::Manager;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .setup(|app| {
            // 初始化数据库
            let db_path = app.path().app_data_dir().unwrap().join("app.db");
            let db = db::Database::new(db_path.to_str().unwrap()).unwrap();

            // 初始化配置
            let config = config::AppConfig::default();

            // 初始化HTTP客户端
            let http_client = reqwest::Client::new();

            // 注册状态
            app.manage(db);
            app.manage(config);
            app.manage(http_client);

            Ok(())
        })
        .invoke_handler(tauri::generate_handler![
            // 项目命令
            commands::project::create_project,
            commands::project::get_projects,
            commands::project::update_project,
            commands::project::delete_project,
            // 文件命令
            commands::file::create_file,
            commands::file::get_files_by_project,
            commands::file::update_file_category,
            commands::file::delete_file,
            // AI命令
            commands::ai::chat_with_ai,
            commands::ai::get_conversation_history,
            commands::ai::clear_conversation_history,
            // 分类命令
            commands::classify::classify_file,
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 4: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 5: 提交代码**

```bash
git add src-tauri/src/commands/classify.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat: 添加文件分类命令"
```

---

## Task 6: 创建配置服务

**Files:**
- Create: `src-tauri/src/services/config_service.rs`

- [ ] **Step 1: 实现配置服务**

```rust
// src-tauri/src/services/config_service.rs
use serde::{Deserialize, Serialize};
use std::path::PathBuf;

/// 应用配置（可序列化版本）
#[derive(Debug, Serialize, Deserialize)]
pub struct AppConfigSerializable {
    pub zhipu_api_key: String,
    pub zhipu_api_url: String,
    pub model_name: String,
    pub classification_prompt: String,
}

impl Default for AppConfigSerializable {
    fn default() -> Self {
        Self {
            zhipu_api_key: "da2c0511ffbc4abe8f66533c6c35f1e6.Xs2mvQ1RMYdDq7Se".to_string(),
            zhipu_api_url: "https://open.bigmodel.cn/api/paas/v4/chat/completions".to_string(),
            model_name: "glm-4.7-flash".to_string(),
            classification_prompt: r#"你是一个专业的文档分类专家。请根据以下文档内容，判断它属于哪个类别：

类别：
- 售前：销售资料、客户沟通、报价单
- 启动：项目启动会、章程、团队组建
- 需求：需求文档、用户故事、用例
- 方案：技术方案、架构设计、选型
- 构建：开发文档、代码规范、接口定义
- 测试：测试用例、测试报告、缺陷
- 上线：部署文档、发布说明、运维
- 验收：验收标准、验收报告、签字
- 转客户成功：交接文档、培训资料、FAQ
- 关闭：项目总结、复盘、归档

文档内容：
{content}

请返回以下JSON格式：
{
  "category": "类别名称",
  "confidence": 0.95,
  "summary": "文档内容摘要（50字以内）",
  "file_type": "文件类型（如：需求文档、测试报告等）"
}"#.to_string(),
        }
    }
}

/// 获取配置文件路径
fn get_config_path() -> Result<PathBuf, String> {
    let config_dir = dirs::config_dir()
        .ok_or_else(|| "无法获取配置目录".to_string())?
        .join("ai-project-manager");

    std::fs::create_dir_all(&config_dir)
        .map_err(|e| format!("创建配置目录失败: {}", e))?;

    Ok(config_dir.join("config.json"))
}

/// 加载配置
pub fn load_config() -> Result<AppConfigSerializable, String> {
    let config_path = get_config_path()?;

    if !config_path.exists() {
        // 如果配置文件不存在，返回默认配置
        return Ok(AppConfigSerializable::default());
    }

    let content = std::fs::read_to_string(&config_path)
        .map_err(|e| format!("读取配置文件失败: {}", e))?;

    let config: AppConfigSerializable = serde_json::from_str(&content)
        .map_err(|e| format!("解析配置文件失败: {}", e))?;

    Ok(config)
}

/// 保存配置
pub fn save_config(config: &AppConfigSerializable) -> Result<(), String> {
    let config_path = get_config_path()?;

    let content = serde_json::to_string_pretty(config)
        .map_err(|e| format!("序列化配置失败: {}", e))?;

    std::fs::write(&config_path, content)
        .map_err(|e| format!("写入配置文件失败: {}", e))?;

    eprintln!("[CONFIG] 配置已保存到: {:?}", config_path);

    Ok(())
}

/// 恢复默认配置
pub fn reset_config() -> Result<AppConfigSerializable, String> {
    let default_config = AppConfigSerializable::default();
    save_config(&default_config)?;
    Ok(default_config)
}
```

- [ ] **Step 2: 添加dirs依赖**

```toml
# src-tauri/Cargo.toml
[dependencies]
# ... existing dependencies
dirs = "5"
```

- [ ] **Step 3: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 4: 提交代码**

```bash
git add src-tauri/src/services/config_service.rs src-tauri/Cargo.toml
git commit -m "feat: 添加配置服务"
```

---

## Task 7: 创建配置命令

**Files:**
- Create: `src-tauri/src/commands/config.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 创建配置命令**

```rust
// src-tauri/src/commands/config.rs
use crate::services::config_service::{self, AppConfigSerializable};

#[tauri::command]
pub async fn get_config() -> Result<AppConfigSerializable, String> {
    config_service::load_config()
}

#[tauri::command]
pub async fn save_config(config: AppConfigSerializable) -> Result<(), String> {
    config_service::save_config(&config)
}

#[tauri::command]
pub async fn reset_config() -> Result<AppConfigSerializable, String> {
    config_service::reset_config()
}
```

- [ ] **Step 2: 更新commands/mod.rs**

```rust
// src-tauri/src/commands/mod.rs
pub mod ai;
pub mod classify;
pub mod config;
pub mod file;
pub mod project;
```

- [ ] **Step 3: 更新lib.rs，注册新命令**

```rust
// src-tauri/src/lib.rs
// ... 在invoke_handler中添加
commands::config::get_config,
commands::config::save_config,
commands::config::reset_config,
```

- [ ] **Step 4: 编译验证**

```bash
cd src-tauri && cargo check
```

Expected: 编译成功，无错误

- [ ] **Step 5: 提交代码**

```bash
git add src-tauri/src/commands/config.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat: 添加配置命令"
```

---

## Task 8: 前端类型定义

**Files:**
- Modify: `src/types/index.ts`

- [ ] **Step 1: 添加新类型定义**

```typescript
// src/types/index.ts

// ... existing types

/** 文件分类结果 */
export interface ClassificationResult {
  category: string;
  confidence: number;
  summary: string;
  file_type: string;
}

/** 应用配置 */
export interface AppConfig {
  zhipu_api_key: string;
  zhipu_api_url: string;
  model_name: string;
  classification_prompt: string;
}

/** 文件上传请求 */
export interface UploadFileRequest {
  project_id: number;
  name: string;
  path: string;
  category?: string;
  content?: string;  // base64编码
  project_name?: string;
  stage?: string;
}
```

- [ ] **Step 2: 提交代码**

```bash
git add src/types/index.ts
git commit -m "feat: 添加新类型定义"
```

---

## Task 9: 前端配置服务

**Files:**
- Create: `src/services/configService.ts`

- [ ] **Step 1: 创建配置服务**

```typescript
// src/services/configService.ts
import { invoke } from "@tauri-apps/api/core";
import type { AppConfig } from "../types";

/**
 * 获取应用配置
 */
export async function getConfig(): Promise<AppConfig> {
  try {
    const config = await invoke<AppConfig>("get_config");
    return config;
  } catch (error) {
    console.error("Failed to get config:", error);
    throw error;
  }
}

/**
 * 保存应用配置
 */
export async function saveConfig(config: AppConfig): Promise<void> {
  try {
    await invoke("save_config", { config });
  } catch (error) {
    console.error("Failed to save config:", error);
    throw error;
  }
}

/**
 * 恢复默认配置
 */
export async function resetConfig(): Promise<AppConfig> {
  try {
    const config = await invoke<AppConfig>("reset_config");
    return config;
  } catch (error) {
    console.error("Failed to reset config:", error);
    throw error;
  }
}
```

- [ ] **Step 2: 提交代码**

```bash
git add src/services/configService.ts
git commit -m "feat: 添加前端配置服务"
```

---

## Task 10: 前端文件服务更新

**Files:**
- Modify: `src/services/fileService.ts`

- [ ] **Step 1: 添加分类API**

```typescript
// src/services/fileService.ts

// ... existing code

/** 分类结果 */
export interface ClassificationResult {
  category: string;
  confidence: number;
  summary: string;
  file_type: string;
}

/**
 * 对文件进行AI分类
 */
export async function classifyFile(filePath: string): Promise<ClassificationResult> {
  try {
    const result = await invoke<ClassificationResult>("classify_file", {
      filePath,
    });
    return result;
  } catch (error) {
    console.error("Failed to classify file:", error);
    throw error;
  }
}

/**
 * 创建文件（支持内容保存）
 */
export async function createFileWithContent(
  request: CreateFileRequest & {
    content?: string;
    project_name?: string;
    stage?: string;
  }
): Promise<File> {
  try {
    const file = await invoke<File>("create_file", {
      projectId: request.project_id,
      name: request.name,
      path: request.path,
      category: request.category,
      content: request.content,
      projectName: request.project_name,
      stage: request.stage,
    });
    return file;
  } catch (error) {
    console.error("Failed to create file:", error);
    throw error;
  }
}
```

- [ ] **Step 2: 提交代码**

```bash
git add src/services/fileService.ts
git commit -m "feat: 更新文件服务，支持分类和内容保存"
```

---

## Task 11: 创建分类确认对话框

**Files:**
- Create: `src/components/FileManager/ClassifyModal.tsx`

- [ ] **Step 1: 创建分类确认对话框组件**

```tsx
// src/components/FileManager/ClassifyModal.tsx
import { useState, useEffect } from "react";
import { Modal, Button, Spin, Tag, Typography, Space, Select, message } from "antd";
import { CheckCircleOutlined, EditOutlined } from "@ant-design/icons";
import type { ClassificationResult } from "../../types";
import { FILE_CATEGORY_LABELS } from "../../services/fileService";
import type { FileCategory } from "../../services/fileService";

const { Text, Paragraph } = Typography;

interface ClassifyModalProps {
  visible: boolean;
  fileName: string;
  classification: ClassificationResult | null;
  loading: boolean;
  onConfirm: (category: string) => void;
  onCancel: () => void;
}

export default function ClassifyModal({
  visible,
  fileName,
  classification,
  loading,
  onConfirm,
  onCancel,
}: ClassifyModalProps) {
  const [editMode, setEditMode] = useState(false);
  const [selectedCategory, setSelectedCategory] = useState<string>("");

  useEffect(() => {
    if (classification) {
      setSelectedCategory(classification.category);
    }
  }, [classification]);

  const handleConfirm = () => {
    if (selectedCategory) {
      onConfirm(selectedCategory);
    }
  };

  const handleEdit = () => {
    setEditMode(true);
  };

  const handleCancelEdit = () => {
    setEditMode(false);
    if (classification) {
      setSelectedCategory(classification.category);
    }
  };

  return (
    <Modal
      title="AI文件分类"
      open={visible}
      onCancel={onCancel}
      footer={null}
      width={500}
    >
      {loading ? (
        <div style={{ textAlign: "center", padding: "40px 0" }}>
          <Spin size="large" />
          <div style={{ marginTop: 16 }}>
            <Text>正在分析文件内容...</Text>
          </div>
        </div>
      ) : classification ? (
        <div>
          <div style={{ marginBottom: 16 }}>
            <Text strong>文件名：</Text>
            <Text>{fileName}</Text>
          </div>

          <div style={{ marginBottom: 16 }}>
            <Text strong>文件类型：</Text>
            <Tag color="blue">{classification.file_type}</Tag>
          </div>

          <div style={{ marginBottom: 16 }}>
            <Text strong>内容摘要：</Text>
            <Paragraph ellipsis={{ rows: 2, expandable: true }}>
              {classification.summary}
            </Paragraph>
          </div>

          <div style={{ marginBottom: 16 }}>
            <Text strong>AI分类建议：</Text>
            <div style={{ marginTop: 8 }}>
              {editMode ? (
                <Select
                  value={selectedCategory}
                  onChange={setSelectedCategory}
                  style={{ width: "100%" }}
                >
                  {(Object.keys(FILE_CATEGORY_LABELS) as FileCategory[]).map((key) => (
                    <Select.Option key={key} value={key}>
                      {FILE_CATEGORY_LABELS[key]}
                    </Select.Option>
                  ))}
                </Select>
              ) : (
                <Tag
                  color="green"
                  icon={<CheckCircleOutlined />}
                  style={{ fontSize: 14, padding: "4px 12px" }}
                >
                  {FILE_CATEGORY_LABELS[selectedCategory as FileCategory] || selectedCategory}
                </Tag>
              )}
            </div>
            <div style={{ marginTop: 8 }}>
              <Text type="secondary">
                置信度：{(classification.confidence * 100).toFixed(0)}%
              </Text>
            </div>
          </div>

          <div style={{ textAlign: "right" }}>
            <Space>
              {editMode ? (
                <>
                  <Button onClick={handleCancelEdit}>取消</Button>
                  <Button type="primary" onClick={handleConfirm}>
                    确认
                  </Button>
                </>
              ) : (
                <>
                  <Button icon={<EditOutlined />} onClick={handleEdit}>
                    修改分类
                  </Button>
                  <Button type="primary" onClick={handleConfirm}>
                    确认使用
                  </Button>
                </>
              )}
            </Space>
          </div>
        </div>
      ) : (
        <div style={{ textAlign: "center", padding: "40px 0" }}>
          <Text type="secondary">无法获取分类结果</Text>
        </div>
      )}
    </Modal>
  );
}
```

- [ ] **Step 2: 提交代码**

```bash
git add src/components/FileManager/ClassifyModal.tsx
git commit -m "feat: 添加分类确认对话框组件"
```

---

## Task 12: 修改文件管理器，集成AI分类

**Files:**
- Modify: `src/components/FileManager/FileManager.tsx`

- [ ] **Step 1: 修改文件上传逻辑，集成AI分类**

```tsx
// src/components/FileManager/FileManager.tsx

// ... existing imports
import { classifyFile, createFileWithContent } from "../../services/fileService";
import type { ClassificationResult } from "../../types";
import ClassifyModal from "./ClassifyModal";

// ... existing code

export default function FileManager({
  projectId,
  onBack,
  onChat,
}: FileManagerProps) {
  // ... existing state

  // 新增：分类相关状态
  const [classifyModalVisible, setClassifyModalVisible] = useState(false);
  const [classifyLoading, setClassifyLoading] = useState(false);
  const [classification, setClassification] = useState<ClassificationResult | null>(null);
  const [currentUploadFile, setCurrentUploadFile] = useState<UploadFile | null>(null);

  // ... existing code

  /** 处理文件上传（带AI分类） */
  const handleUpload = async () => {
    if (!selectedStage || uploadFileList.length === 0) return;

    let successCount = 0;
    let failCount = 0;

    for (const file of uploadFileList) {
      if (file.originFileObj) {
        try {
          // 读取文件内容
          const content = await readFileAsBase64(file.originFileObj);

          // 创建文件（保存到磁盘）
          const createdFile = await createFileWithContent({
            project_id: projectId,
            name: file.name ?? "未知文件",
            path: `/projects/${projectId}/${file.name}`,
            category: selectedStage as FileCategory,
            content: content,
            project_name: `project_${projectId}`,  // 需要从项目信息获取
            stage: selectedStage,
          });

          // 调用AI分类
          setCurrentUploadFile(file);
          setClassifyLoading(true);
          setClassifyModalVisible(true);

          try {
            const result = await classifyFile(createdFile.path);
            setClassification(result);
            setClassifyLoading(false);

            // 等待用户确认分类
            const userCategory = await waitForUserConfirmation();

            // 更新文件分类
            if (userCategory) {
              await updateFileCategory({
                id: createdFile.id!,
                category: userCategory as FileCategory,
              });
            }
          } catch (classifyError) {
            console.error("AI分类失败:", classifyError);
            setClassifyLoading(false);
            // 分类失败时，保持原分类
          }

          successCount++;
        } catch (error) {
          console.error("文件上传失败:", error);
          failCount++;
        }
      }
    }

    // 显示结果
    if (failCount === 0) {
      message.success(`成功上传 ${successCount} 个文件`);
    } else if (successCount === 0) {
      message.error(`全部 ${failCount} 个文件上传失败`);
    } else {
      message.warning(`上传完成：${successCount} 个成功，${failCount} 个失败`);
    }

    setUploadModalOpen(false);
    setUploadFileList([]);
    await loadFiles();
  };

  /** 读取文件为base64 */
  const readFileAsBase64 = (file: File): Promise<string> => {
    return new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => {
        const base64 = (reader.result as string).split(",")[1];
        resolve(base64);
      };
      reader.onerror = reject;
      reader.readAsDataURL(file);
    });
  };

  /** 等待用户确认分类 */
  const waitForUserConfirmation = (): Promise<string | null> => {
    return new Promise((resolve) => {
      // 这个promise会在用户点击确认或取消时resolve
      window.__classifyResolve = resolve;
    });
  };

  /** 处理分类确认 */
  const handleClassifyConfirm = (category: string) => {
    setClassifyModalVisible(false);
    setClassification(null);
    setCurrentUploadFile(null);
    if (window.__classifyResolve) {
      window.__classifyResolve(category);
      delete window.__classifyResolve;
    }
  };

  /** 处理分类取消 */
  const handleClassifyCancel = () => {
    setClassifyModalVisible(false);
    setClassification(null);
    setCurrentUploadFile(null);
    if (window.__classifyResolve) {
      window.__classifyResolve(null);
      delete window.__classifyResolve;
    }
  };

  // ... existing code

  return (
    <div>
      {/* ... existing code */}

      {/* 分类确认对话框 */}
      <ClassifyModal
        visible={classifyModalVisible}
        fileName={currentUploadFile?.name || ""}
        classification={classification}
        loading={classifyLoading}
        onConfirm={handleClassifyConfirm}
        onCancel={handleClassifyCancel}
      />
    </div>
  );
}
```

- [ ] **Step 2: 提交代码**

```bash
git add src/components/FileManager/FileManager.tsx
git commit -m "feat: 文件管理器集成AI分类功能"
```

---

## Task 13: 创建设置页面

**Files:**
- Create: `src/components/Settings/SettingsPage.tsx`
- Create: `src/components/Settings/PromptEditor.tsx`
- Modify: `src/App.tsx`

- [ ] **Step 1: 创建Prompt编辑器组件**

```tsx
// src/components/Settings/PromptEditor.tsx
import { useState, useEffect } from "react";
import { Input, Button, message, Space, Typography } from "antd";
import { SaveOutlined, UndoOutlined } from "@ant-design/icons";

const { TextArea } = Input;
const { Text } = Typography;

interface PromptEditorProps {
  value: string;
  onChange: (value: string) => void;
  onReset: () => void;
}

export default function PromptEditor({
  value,
  onChange,
  onReset,
}: PromptEditorProps) {
  const [localValue, setLocalValue] = useState(value);

  useEffect(() => {
    setLocalValue(value);
  }, [value]);

  const handleChange = (e: React.ChangeEvent<HTMLTextAreaElement>) => {
    setLocalValue(e.target.value);
  };

  const handleSave = () => {
    onChange(localValue);
    message.success("Prompt已保存");
  };

  const handleReset = () => {
    onReset();
    message.success("已恢复默认Prompt");
  };

  return (
    <div>
      <div style={{ marginBottom: 8 }}>
        <Text type="secondary">
          编辑分类Prompt，使用 {"{content}"} 作为文件内容占位符
        </Text>
      </div>
      <TextArea
        value={localValue}
        onChange={handleChange}
        rows={15}
        style={{ fontFamily: "monospace" }}
      />
      <div style={{ marginTop: 16 }}>
        <Space>
          <Button
            type="primary"
            icon={<SaveOutlined />}
            onClick={handleSave}
          >
            保存
          </Button>
          <Button
            icon={<UndoOutlined />}
            onClick={handleReset}
          >
            恢复默认
          </Button>
        </Space>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 创建设置页面组件**

```tsx
// src/components/Settings/SettingsPage.tsx
import { useState, useEffect } from "react";
import { Card, Form, Input, Button, message, Spin, Space, Typography } from "antd";
import { SaveOutlined, ArrowLeftOutlined } from "@ant-design/icons";
import type { AppConfig } from "../../types";
import { getConfig, saveConfig, resetConfig } from "../../services/configService";
import PromptEditor from "./PromptEditor";

const { Title } = Typography;

interface SettingsPageProps {
  onBack?: () => void;
}

export default function SettingsPage({ onBack }: SettingsPageProps) {
  const [config, setConfig] = useState<AppConfig | null>(null);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);

  const [form] = Form.useForm();

  useEffect(() => {
    loadConfig();
  }, []);

  const loadConfig = async () => {
    setLoading(true);
    try {
      const data = await getConfig();
      setConfig(data);
      form.setFieldsValue(data);
    } catch (error) {
      message.error("加载配置失败");
    } finally {
      setLoading(false);
    }
  };

  const handleSave = async () => {
    setSaving(true);
    try {
      const values = await form.validateFields();
      await saveConfig(values);
      setConfig(values);
      message.success("配置已保存");
    } catch (error) {
      message.error("保存配置失败");
    } finally {
      setSaving(false);
    }
  };

  const handleReset = async () => {
    try {
      const defaultConfig = await resetConfig();
      setConfig(defaultConfig);
      form.setFieldsValue(defaultConfig);
      message.success("已恢复默认配置");
    } catch (error) {
      message.error("恢复默认配置失败");
    }
  };

  const handlePromptChange = (value: string) => {
    form.setFieldValue("classification_prompt", value);
  };

  const handlePromptReset = async () => {
    try {
      const defaultConfig = await resetConfig();
      form.setFieldValue("classification_prompt", defaultConfig.classification_prompt);
    } catch (error) {
      message.error("恢复默认Prompt失败");
    }
  };

  if (loading) {
    return (
      <div style={{ textAlign: "center", padding: "100px 0" }}>
        <Spin size="large" />
      </div>
    );
  }

  return (
    <div>
      <div style={{ marginBottom: 16 }}>
        <Space>
          <Button icon={<ArrowLeftOutlined />} onClick={onBack}>
            返回
          </Button>
          <Title level={3} style={{ margin: 0 }}>
            设置
          </Title>
        </Space>
      </div>

      <Card title="AI配置" style={{ marginBottom: 16 }}>
        <Form form={form} layout="vertical">
          <Form.Item
            name="zhipu_api_key"
            label="智谱AI API Key"
            rules={[{ required: true, message: "请输入API Key" }]}
          >
            <Input.Password />
          </Form.Item>

          <Form.Item
            name="zhipu_api_url"
            label="API端点"
            rules={[{ required: true, message: "请输入API端点" }]}
          >
            <Input />
          </Form.Item>

          <Form.Item
            name="model_name"
            label="模型名称"
            rules={[{ required: true, message: "请输入模型名称" }]}
          >
            <Input />
          </Form.Item>
        </Form>
      </Card>

      <Card title="分类Prompt配置">
        <Form.Item
          name="classification_prompt"
          rules={[{ required: true, message: "请输入分类Prompt" }]}
        >
          <PromptEditor
            value={config?.classification_prompt || ""}
            onChange={handlePromptChange}
            onReset={handlePromptReset}
          />
        </Form.Item>
      </Card>

      <div style={{ marginTop: 16, textAlign: "right" }}>
        <Space>
          <Button onClick={handleReset}>
            恢复默认配置
          </Button>
          <Button
            type="primary"
            icon={<SaveOutlined />}
            onClick={handleSave}
            loading={saving}
          >
            保存配置
          </Button>
        </Space>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 更新App.tsx，添加设置路由**

```tsx
// src/App.tsx

// ... existing imports
import SettingsPage from "./components/Settings/SettingsPage";

// ... existing code

function App() {
  // ... existing state

  // 添加设置页面状态
  const [showSettings, setShowSettings] = useState(false);

  // ... existing code

  // 渲染设置页面
  if (showSettings) {
    return (
      <div style={{ padding: 24 }}>
        <SettingsPage onBack={() => setShowSettings(false)} />
      </div>
    );
  }

  // ... existing rendering logic

  return (
    <div style={{ padding: 24 }}>
      {/* 添加设置按钮 */}
      <div style={{ marginBottom: 16, textAlign: "right" }}>
        <Button onClick={() => setShowSettings(true)}>
          设置
        </Button>
      </div>

      {/* ... existing content */}
    </div>
  );
}

export default App;
```

- [ ] **Step 4: 提交代码**

```bash
git add src/components/Settings/SettingsPage.tsx src/components/Settings/PromptEditor.tsx src/App.tsx
git commit -m "feat: 添加设置页面和Prompt编辑器"
```

---

## Task 14: 集成测试

**Files:**
- Test: 手动测试

- [ ] **Step 1: 编译并运行应用**

```bash
# 编译后端
cd src-tauri && cargo build

# 运行前端
cd .. && npm run dev

# 或者运行Tauri应用
cd src-tauri && cargo tauri dev
```

- [ ] **Step 2: 测试自动创建文件夹**

1. 启动应用
2. 创建一个新项目
3. 检查 `projects/` 目录下是否创建了对应的文件夹
4. 验证10个子文件夹是否存在

Expected: 文件夹结构正确创建

- [ ] **Step 3: 测试文件上传和存储**

1. 进入项目文件管理页面
2. 选择一个阶段（如"需求"）
3. 上传一个txt文件
4. 检查文件是否保存到 `projects/{项目名}/需求/` 目录

Expected: 文件成功保存到磁盘

- [ ] **Step 4: 测试AI分类**

1. 上传一个包含需求内容的txt文件
2. 观察是否弹出分类确认对话框
3. 检查AI分类建议是否正确
4. 点击"确认使用"或"修改分类"

Expected: AI正确分类文件，对话框正常工作

- [ ] **Step 5: 测试设置页面**

1. 点击"设置"按钮
2. 修改API Key
3. 修改分类Prompt
4. 点击"保存配置"
5. 重新进入设置页面，验证配置是否保存

Expected: 配置成功保存和加载

- [ ] **Step 6: 提交最终代码**

```bash
git add .
git commit -m "feat: P1功能完成 - 自动创建文件夹、文件存储、AI分类、设置页面"
```

---

## 自检结果

✅ **Spec覆盖**：所有设计文档中的需求都有对应任务
✅ **Placeholder扫描**：无TBD、TODO或不完整部分
✅ **类型一致性**：所有类型、方法签名、属性名称保持一致

---

## 执行选项

计划完成并保存到 `docs/superpowers/plans/2026-06-04-p1-features.md`。

两种执行方式：

**1. Subagent-Driven（推荐）** - 每个任务分派一个独立子代理，任务间进行审查，快速迭代

**2. Inline Execution** - 在当前会话中执行任务，批量执行并设置检查点

选择哪种方式？
